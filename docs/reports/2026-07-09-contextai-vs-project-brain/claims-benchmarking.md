### CLAIM: project-brain은 형태소 토큰화(mecab-ko→kiwi→regex)한 본문을 FTS5 BM25로 색인하고 색인·검색이 같은 토큰화를 공유하는 반면, ContextAI는 BM25/tsvector가 없고 형태소 분석기도 없으며 어휘 매칭이 본문이 아닌 제목/경로/id 메타에만 LIKE로 걸린다.
- 판정: CONFIRMED
- 근거: project-brain: tokenize_ko.py:108-116 (_resolve_backend 폴백 사다리 mecab-ko→kiwipiepy→regex), docstring :3 "단일 공유 함수 tokenize() 하나만 색인과 쿼리가 함께 쓴다"; search_index.py:95 (CREATE VIRTUAL TABLE documents_fts USING fts5), :154+:168 (색인측 tokenize(surface)→documents_fts), :437+:448+:469 (search_bm25가 tokenize(query)로 토큰화 후 bm25(documents_fts)), :452-459 (색인/쿼리 토크나이저 비대칭 경고 가드). ContextAI: 전체 레포 grep에서 tsvector/to_tsquery/to_tsvector/ts_rank/plainto_tsquery/websearch_to_tsquery 0건; wiki_index_store.py:1506-1563 search()는 asimilarity_search(dense pgvector, :1538) + _identity_search를 RRF 병합(:1563); _identity_terms :1341-1350은 query.casefold()+re.findall(r"\w+") — 형태소 분석 없음(text.py:296-305 동일); confluence_client.py:183-184 "avoid pulling a tokenizer into the indexing path"; LIKE 어휘매칭은 providers.py:1071 metadata->>field 대상, docstring :1055 "without reading chunk bodies", 가중 필드 title/heading_path/heading_anchor/page_id/source(wiki_index_store.py:1374-1388,1443) 및 title/source/text_id(text.py:308-320) — 본문 아닌 제목/경로/id 메타뿐. 반례 조사: jira.py:112 순수 asimilarity_search, code.py:4 "Does NOT use vector search"(에이전트 Grep 툴, 색인 아님) — 반박 없음.
- 정정: -
- 왜 중요: 이 대비가 틀리면 '한국어 검색에서 project-brain이 앞선다'는 최대 우위(L1)와 '품질이 밀리지 않는다'는 판단의 핵심 근거가 무너진다. project-brain 코드(tokenize_ko.py:100-114, search_index.py:95)로 확인, ContextAI는 보고서 A.

### CLAIM: ContextAI는 커밋되던 골든셋 A~E 평가를 2026-05-15(098bc1bd)에 삭제해 현재 커밋된 품질 베이스라인이 0건이고 검색 관련성 결정론 테스트가 없는 반면, project-brain은 eval_scenarios.json 골든셋 + 556개 결정론 합성 테스트를 유지한다.
- 판정: CONFIRMED
- 근거: ContextAI 삭제: commit 098bc1bd "Remove legacy query API", Date Fri May 15 15:59:39 2026 +0900 (git show 098bc1bd --stat). evals/regression/ 전체 삭제 — create_dataset.py, GUIDE.md, TEST_CASES_TEMPLATE.md, run_experiment.py, HTML 리포트 6개, 총 7628 deletions. 삭제 전 create_dataset.py(098bc1bd~1)의 TEST_CASES 카테고리: "# A. Knowledge 검색"(A1,A5,A6,A7,A_IMON2), "# B. Dual-Source"(B1,B3), "# C. Code 검색"(C1,C2), "# E. 엣지 케이스"(E1,E2,E3,E5,E8). 최초 추가는 4c09ffd8, 재추가 없음(git log --follow).
현재 커밋 베이스라인 0: git ls-files상 커밋된 golden/dataset json 없음. evals/에는 auto_review.py+triage_ui.html만 남고, auto_review.py는 Langfuse 라이브 trace를 읽음(evals/auto_review.py:485 langfuse.api.trace.list, :499 .get) — 커밋된 기준셋 아님. tests/regression/는 xfail known-gaps(README: "may be empty").
project-brain: pytest tests/ --co 결과 정확히 556 tests collected; ROADMAP.md:291 "엔진 합성 556 통과".
※ 두 가지 뉘앙스(판정을 뒤집진 않음): (1) ContextAI에 검색 결정론 테스트가 아예 없는 건 아님 — tests/unit/test_knowledge_freshness_order.py(:100,:141 최신순 정렬 단정)와 test_knowledge_filter_fallback.py(LLM 관련성 필터 drop/retry)는 결정론적이지만 '관련성/recall 랭킹 품질'이 아니라 '최신순 정렬·실패 시맨틱'만 고정함. 따라서 "검색 관련성 결정론 테스트 없음"은 엄격 해석(관련성 랭킹 골든)에선 성립하나 부정확한 표현. (2) eval_scenarios.json은 엔진 레포에 없음(git ls-files 미검출) — 2-레포 모델상 데이터 레포 소유로 커밋·추적됨(CLAUDE.md:15, README.md:18 "프로젝트 git이 추적한다"), 이 엔진 클론에서 직접 확인 불가하나 문서·ROADMAP(bb2 eval 15/15)로 존재 확인.
- 정정: -
- 왜 중요: 이게 틀리면 '품질 비교가 실측 불가'라는 정직성 논거와 '결정론 eval에서 우리가 앞선다'(L2)가 흔들린다. project-brain 556 테스트·eval_harness.py 존재 확인, ContextAI는 보고서 C.

### CLAIM: ContextAI는 로컬 작업 디렉토리를 색인할 소스가 없고(텍스트 붙여넣기 또는 도달 가능 git 원격 push만) 사내 인프라(FCP/Athenz/OpenAI egress/CorporateIdP)가 전제인 반면, project-brain은 서버·계정 없는 순수 로컬 CLI + git 추적 데이터다.
- 판정: CONFIRMED
- 근거: ContextAI 로컬 색인 소스 없음: 소스 타입은 wiki(Confluence 원격)·code·text뿐(README.md:7-8, db/source_configs.py). code 소스는 원격 git_url만 clone하며 sources/repo.py:336-427(Repo.clone_from/fetch) + sources/github_app.py:218-220(is_host_allowed: URL hostname이 GHE_ALLOWED_HOSTS에 있어야 함, 로컬경로·file://는 hostname 빈값→거부) + GitHub App installation token(create_repo_token). 로컬 디렉토리 적재 경로 자체가 없음. text 소스는 붙여넣기 전용(db/source_configs.py:120-126 "raw text content that users paste directly"). 사내 인프라 전제: Athenz/Amaterasu mTLS(README.md:9-35, config.py:446-453), FCP(config.py:286 fcp_proxy_url, README.md:86 X-Unified-ID/X-Athenz-Principal FCP 주입·X-Forwarded-* 불신), OpenAI egress(providers.py:16,334,357 langchain_openai ChatOpenAI/OpenAIEmbeddings base_url=openai_base_url; hack/base.yaml:28 "OpenAI proxy (LY corp egress)"), CorporateIdP(README.md:86 X-Employee-ID, .env.template:27 CORPORATEIDP_CLIENT_SECRET); +PostgreSQL·oauth2-proxy 필수, git remote=git.linecorp.com. project-brain은 순수 로컬 CLI: uv tool install -e 로컬 설치·entrypoint project_brain.cli:main(pyproject.toml:18, README.md:28-30), 의존성=numpy/sqlite-vec/sentence-transformers/kiwipiepy만(pyproject.toml:6-15, HTTP/server/auth/openai 라이브러리 0개·src grep 무결과), 임베딩 로컬 bge-m3(embedder.py:29,66-90), 저장소 로컬 SQLite(.brain-local/index.db), 데이터는 프로젝트 git 추적(README.md:17-18).
- 정정: 주장 자체는 성립하나 한 가지 정밀화: hack/base.yaml:29의 openai_base_url 값은 https://us.api.openai.com/v1 (OpenAI 자체 리전 엔드포인트)라서 "egress"는 OpenAI에 도달하는 사내 네트워크 경로를 뜻하지, 내부 프록시 호스트로의 우회를 뜻하지는 않는다. 코드 주석("LY corp egress")과 OpenAI 외부 LLM 의존이라는 핵심은 그대로 유효해 판정에는 영향 없음.
- 왜 중요: 이 사실이 두 도구가 '공존 관계'이고 '회사 표준화가 개인 로컬 루프를 대체 못 한다'는 질문 3의 핵심 결론을 지탱한다. ContextAI는 보고서 E, project-brain은 보고서 G.

### CLAIM: project-brain의 raw 청커는 이미 마크다운 헤더로 섹션을 가르지만(_split_sections/_HEADING_RE) 청크에 heading 경로(breadcrumb)를 접두하지 않고 청크는 {chunk_id, context_id, text}뿐이며, ContextAI wiki 청커는 breadcrumb + 섹션 앵커를 붙인다.
- 판정: CONFIRMED
- 근거: project-brain raw 청커: src/project_brain/raw_chunks.py:25 (_HEADING_RE = r"^#{1,6}\s"), :39-55 (_split_sections 헤더로 섹션 분할), :91-104 (split_markdown이 섹션/겹침창 텍스트만 append — breadcrumb 접두 없음), :107-126 (iter_raw_sources가 {chunk_id, context_id, text} 정확히 3키만 반환; docstring :110, dict :121-125). src/ 전체에 breadcrumb|heading_path|heading_anchor|section_path|section_anchor rg 결과 매치 0(exit 1). 소비처 src/project_brain/search_index.py:178-200,301-304는 ch["chunk_id"]/["context_id"]/["text"]만 사용. ContextAI 위키 청커: src/context_ai/sources/confluence_client.py:446 chunk_by_headings가 {page_content, heading_anchor, heading_path} 반환, :459-464 docstring(heading_anchor=렌더 섹션 앵커 딥링크 id, heading_path="Page > H1 > H2" breadcrumb를 청크에 접두), 실제 접두 :524/:528 (f"{breadcrumb}\n\n{body}"), 앵커 부착 :530.
- 정정: 주장의 모든 구성요소가 코드로 직접 확인됨. 다만 정직하게 덧붙일 뉘앙스: project-brain의 _split_sections는 섹션의 "직속 헤더 한 줄"은 텍스트 안에 그대로 남긴다(raw_chunks.py:52). 즉 즉시 헤더 자체는 마크다운 일부로 존재한다. 하지만 상위 헤더들을 엮은 breadcrumb 경로를 만들거나 접두하는 동작은 전혀 없고, 과대 섹션의 2번째 이후 겹침창은 그 직속 헤더마저 잃는다. 반대로 ContextAI는 헤더 자체 텍스트를 본문에서 떼어내(:511-512) 전용 breadcrumb로 옮긴다. 이 차이는 주장을 반박하지 않고 오히려 뒷받침한다.
- 왜 중요: 가장 값어치 있는 채택 후보 B2가 '이미 다 됐다'도 '전혀 없다'도 아닌 '헤더 분리는 됐고 breadcrumb 접두만 빠졌다'는 좁은 갭임을 규정한다. 틀리면 채택 항목의 비용·우선순위 판정이 바뀐다. raw_chunks.py:39-55 확인, ContextAI는 보고서 A.

### CLAIM: project-brain의 router는 스키마에 있는 acl/sensitivity 필드를 읽지 않아(grep 0건) 사용자별 권한이 실집행되지 않으며, 이는 단일 사용자 설계상 의도된 것이다.
- 판정: CONFIRMED
- 근거: router.py에서 acl/sensitivity를 읽는 코드 0건: `rg "acl|sensitivity" src/project_brain/router.py` → exit 1(무매치). 유일한 접근 게이트 `_restricted_for`(router.py:758-769)는 EvidenceManifest의 `redaction_status != "approved"`만 검사(코퍼스 전역, 사용자 무관, fail-closed). acl/sensitivity는 assembly.py:168-169에서 매니페스트 기본값을 '쓸' 때만 참조되고 조회 시점엔 아무도 읽지 않음(레포 전역 grep 확인). 사용자별 권한: QueryRouter.__init__(router.py:32-56)·answer(router.py:80)에 requester/principal/user 식별자 인자 자체가 없음 — src/ 전역에 사용자 신원 개념 부재. tests/test_router.py:55-76은 게이트가 redaction_status 전용임을 확증(acl 세팅해도 acl 기반 제한 미검증). 단일 사용자 의도: deferred-items §4(line 155) "협업자 0명이라 권한 게이트가 no-op", design-canonical §4(line 131) "팀 확장 시 ... 팀 공개 시점에". ACL enforcement는 query-routing spec §2 Excluded(line 38)·§12 Deferred(line 349 "Needs separate privacy/redaction policy"), object-model §16(line 767), storage-layout(line 249 "before shared Brain")에 명시적으로 미룬 항목. 참고: ContextAI(별도 제품)는 binder_access·wiki_query_access 등 실접근제어를 갖지만 이 판정 대상은 project-brain이라 무관.
- 정정: 주장 자체는 성립하나 뉘앙스 하나 보정: acl/sensitivity 비집행은 '영구 단일 사용자 전용 설계'가 아니라 단일 사용자 PoC 단계에서 의도적으로 '미뤄둔(deferred)' 기능이다(문서상 "shared Brain 전에 privacy/redaction 연구 필요"로 재검토 예정). 또 접근 통제가 전무한 건 아니다 — redaction_status 기반 restricted 라벨링은 코퍼스 전역으로 실제 집행된다. 즉 '사용자별 acl/sensitivity만' 휴면이고, 그게 현재 협업자 0명 상태의 의도된 no-op라는 점은 사실.
- 왜 중요: 이게 사실이라야 'ContextAI의 멀티유저 권한 모델은 이식할 갭이 아니라 애초에 다른 용도'라는 기각 논거(R2)가 성립한다. 틀리면 권한 모델이 진짜 결함이 되어 벤치마킹 우선순위가 뒤집힌다. router.py grep·schema.py:12로 확인.
