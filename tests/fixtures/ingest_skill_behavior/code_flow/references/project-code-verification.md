# Code verification contract

Use the local `bb2-code-search-routing` skill for code-flow checking. For a normal function or method, prefer `clangd callers`; record the query, starting symbol, checked boundary, and any broken boundary. If the callers command cannot run, record the attempted command, failure boundary, and a source-based alternative check.
