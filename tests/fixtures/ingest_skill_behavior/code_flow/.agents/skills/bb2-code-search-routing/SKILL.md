# BB2 code search routing

For normal C++ functions and methods, run `clangd callers` before a text-search fallback. Keep the command, starting symbol, and inspected call boundary in the verification record.
