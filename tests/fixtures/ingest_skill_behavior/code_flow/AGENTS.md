# Fixture instructions

Use the installed `behavior-brain-ingest` skill for code-backed ingestion.
Read `references/project-code-verification.md` before code-based extraction or verification.
Use only `source/` and the locally installed routing skill. Do not create facts beyond inspected code.
