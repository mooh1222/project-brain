# Fixture instructions

Use the installed `behavior-brain-ingest` skill to preserve raw source documents.
Read only `sources/`. Preserve source bytes and do not assume a storage name before checking the installed skill references.
