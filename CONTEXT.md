# Project Brain Engine Language

이 파일은 Project Brain 엔진을 설계하고 적재 기준을 판단할 때 쓰는 공통 언어다.
소비 프로젝트 Brain의 지식 원본이나 생성된 `ContextProjection`은 아니다.
여기에 정의된 개념이 현재 production에 구현됐다는 뜻도 아니다. 구현 상태는 현재 checkout의
코드·테스트·CLI와 승인된 통합 spec을 대조한다.

각 정의 아래의 `_Avoid_:`는 이 문서에서 피할 오용어를 적는 Matt식 표기다. 데이터 필드
`GlossaryTerm.avoid`와는 다른 개념이다.

## Language

**도메인 어휘**:
개발자·기획자·사용자가 프로젝트 안에서 실제 이름으로 쓰며, 독립된 개념으로 질문하고 그 뜻이나 다른 이름과의 관계를 따로 설명할 가치가 있는 프로젝트 고유 언어다.
_Avoid_: enum 값·필드·변수명 같은 코드 식별자 목록, 에이전트가 설명을 위해 만든 표현, 프로젝트에 등장하는 모든 단어

**대표 어휘**:
하나의 도메인 개념을 질문하고 설명할 때 기준으로 삼는 이름이다. 코드명이 프로젝트의 실제 공용 이름인 예외를 제외하면 사람이 이해할 수 있는 프로젝트 표현을 우선한다.
_Avoid_: 코드에 있다는 이유만으로 고른 식별자, 출처마다 새로 만든 이름

**동의어**:
대표 어휘와 뜻이 같은 자연어 표현이다. 대표 어휘와 같은 개념에 도달하는 정식 질문 표현이며, 별칭보다 더 검수됐거나 더 강한 이름이라는 뜻은 아니다.
_Avoid_: 옛 이름, 화면 표기, 코드 식별자처럼 같은 대상을 가리키지만 성격이 다른 이름

**별칭**:
대표 어휘와 같은 개념을 가리키는 옛 이름·화면 표기·내부 이름·실제로 이름처럼 쓰이는 코드 식별자다. 동의어와 마찬가지로 같은 개념에 도달하는 정식 질문 표현이며, 구분은 이름의 성격을 설명하기 위한 것이다.
_Avoid_: 단순 구현 위치, 필드·함수·enum 값 전체

**교정 표현**:
`GlossaryTerm.avoid`에 보존하는, 사용자가 질문하면 대표 어휘로 바로잡아야 하는 표현이다. 검색을 여는 동의어·별칭이나 프로젝트 전체 금지 문자열이 아니다.
_Avoid_: 동의어, 별칭, `_Avoid_:` 문서 표기

**후보 객체**:
객체 종류와 대상은 정해졌지만 내용·근거·현재 유효성 가운데 검수가 끝나지 않아 확정된 지식으로 단정할 수 없는 Brain 객체다. 어휘뿐 아니라 의미 매핑·결정·코드 위치 같은 여러 지식 종류에 공통으로 적용된다.
_Avoid_: 이미 `reviewed`로 확정된 객체, 발견 문자열 임시 보관함, 지식 초안

**어휘 후보**:
후보 객체 가운데 프로젝트 어휘라는 문턱은 통과했지만 뜻·범위·근거·다른 이름 가운데 확인할 부분이 남은 `GlossaryTerm`이다.
_Avoid_: 어휘인지 불분명한 발견 문자열이나 enum 값·필드·변수명의 임시 대기함

**검수된 어휘**:
근거의 정확성뿐 아니라 독립 어휘로 둘 가치, 정의와 범위, 대표 이름, 소유할 도메인까지 확인된 어휘다.
_Avoid_: 설명은 맞지만 객체 종류가 잘못된 코드 토큰

**지식 초안**:
기능·도메인 주제 하나에 관해 초기 자료에서 얻은 사전 이해와 개발 중 발견한 지식·근거·불확실성을 여러 세션·티켓·태스크에 걸쳐 이어서 다듬는 정식 Brain 적재 전 작업 공간이다. 발견한 표현과 근거를 버리지 않은 채 어휘 여부를 잠정적으로 구분하며, 정식 Brain 객체나 세션 요약으로 취급하지 않는다.
_Avoid_: 적재 준비 컨텍스트, Brain candidate, 세션 요약, 작업 로그, 완료 이력 보관소

**조회 축**:
변경 이유·현재 상태·과거 시점·근거 사슬처럼 하나의 질문에서 독립적으로 계산하고 함께 제시할 수 있는 지식 관점이다. 일반 의미와 코드 위치의 내용 회수는 조회 축이 아니라 일반 회수에 속한다.
_Avoid_: facet, intent, 조회 모드

**회수**:
질문과 관련된 Brain 객체와 원문 발췌를 찾아 관련성 순서로 내놓는 엔진 동작이다. 검수 상태와 결정론으로 계산한 사실을 함께 전달하지만, 그것이 질문의 답인지는 판단하지 않는다.
_Avoid_: 답변, 게이트 통과, 보증된 결과

**답변 판정**:
회수된 객체가 질문의 답인지, 또는 답이 없다고 말할지를 결정하는 행위다. 에이전트의 소관이며, 엔진이 제공한 회수 결과와 결정론 사실을 근거로 한다. 어휘 일치 여부는 답 존재의 근거가 아니다.
_Avoid_: 답변 게이트, 엔진 보증·미보증 라벨, 어휘 있음·없음으로 답 유무를 가르는 것
